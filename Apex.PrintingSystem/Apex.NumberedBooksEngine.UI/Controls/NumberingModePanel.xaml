<UserControl x:Class="Apex.NumberedBooksEngine.UI.Controls.NumberingModePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="280"
             Background="White">
    <StackPanel Margin="10">
        <TextBlock Text="Numbering Mode" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        
        <!-- Mode Selection -->
        <RadioButton x:Name="AutoMode" Content="🔍 Automatic Detection" GroupName="Mode" IsChecked="True" Margin="0,5"/>
        <RadioButton x:Name="ShersharaMode" Content="📄 Shershara (Perforated Pads)" GroupName="Mode" Margin="0,5"/>
        <RadioButton x:Name="CuttingMode" Content="✂ Cutting (Imposed Sheets)" GroupName="Mode" Margin="0,5"/>
        <RadioButton x:Name="CustomMode" Content="⚙ Custom Pattern" GroupName="Mode" Margin="0,5"/>
        
        <!-- Mode Description -->
        <Border Background="#F5F5F5" CornerRadius="5" Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="ModeDescription" TextWrapping="Wrap" FontSize="11" Foreground="Gray"
                       Text="Auto-detect mode based on slot layout."/>
        </Border>
        
        <!-- Detected Mode -->
        <StackPanel x:Name="DetectedModePanel" Orientation="Horizontal" Margin="0,10,0,0" Visibility="Visible">
            <TextBlock Text="Detected:" FontWeight="SemiBold" Margin="0,0,5,0"/>
            <TextBlock x:Name="DetectedModeText" Text="Shershara" Foreground="#10B981"/>
        </StackPanel>
    </StackPanel>
</UserControl>
