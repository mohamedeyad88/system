<UserControl x:Class="Apex.NumberedBooksEngine.UI.Controls.NumberingModePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="280"
             Background="#1E293B">
    <StackPanel Margin="10">
        <TextBlock Text="وضع الترقيم" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="White"/>

        <!-- Mode Selection -->
        <RadioButton x:Name="AutoMode" Content="🔍 كشف تلقائي" GroupName="Mode" IsChecked="True" Margin="0,5" Foreground="#E2E8F0"/>
        <RadioButton x:Name="ShersharaMode" Content="📄 ترقيم الشرشرة (دفاتر)" GroupName="Mode" Margin="0,5" Foreground="#E2E8F0"/>
        <RadioButton x:Name="CuttingMode" Content="✂ ترقيم القص (ورق مفروز)" GroupName="Mode" Margin="0,5" Foreground="#E2E8F0"/>
        <RadioButton x:Name="CustomMode" Content="⚙ نمط مخصص" GroupName="Mode" Margin="0,5" Foreground="#E2E8F0"/>

        <!-- Mode Description -->
        <Border Background="#334155" CornerRadius="5" Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="ModeDescription" TextWrapping="Wrap" FontSize="11" Foreground="#94A3B8"
                       Text="كشف تلقائي لوضع الترقيم بناءً على تخطيط الخانات"/>
        </Border>

        <!-- Detected Mode -->
        <StackPanel x:Name="DetectedModePanel" Orientation="Horizontal" Margin="0,10,0,0" Visibility="Visible">
            <TextBlock Text="تم الكشف:" FontWeight="SemiBold" Margin="0,0,5,0" Foreground="#94A3B8"/>
            <TextBlock x:Name="DetectedModeText" Text="شرشرة" Foreground="#10B981"/>
        </StackPanel>
    </StackPanel>
</UserControl>
