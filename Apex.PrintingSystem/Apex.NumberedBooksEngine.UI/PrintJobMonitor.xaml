<Window x:Class="Apex.NumberedBooksEngine.UI.PrintJobMonitor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Print Job Monitor" Height="400" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F9FAFB">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Print Job Progress" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Progress Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <ProgressBar x:Name="ProgressBar" Height="30" Minimum="0" Maximum="100" Value="0" Foreground="#10B981"/>
            <TextBlock x:Name="ProgressText" Text="0%" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </Grid>

        <!-- Stats -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Current Page -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Current Page:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="CurrentPageText" Text="0 / 0" Margin="0,5"/>

            <!-- Speed -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Speed:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="SpeedText" Text="0 pages/sec" Margin="0,5"/>

            <!-- Elapsed Time -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Elapsed Time:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ElapsedTimeText" Text="00:00:00" Margin="0,5"/>

            <!-- Status -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Status:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="StatusText" Text="Idle" Margin="0,5"/>

            <!-- Errors -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Errors:" FontWeight="SemiBold" Margin="0,5"/>
            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="ErrorsText" Text="None" Foreground="#EF4444" Margin="0,5"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="PauseButton" Content="Pause" Width="100" Padding="10,8" Margin="0,0,10,0" Click="Pause_Click" Background="#F59E0B" Foreground="White"/>
            <Button x:Name="ResumeButton" Content="Resume" Width="100" Padding="10,8" Margin="0,0,10,0" Click="Resume_Click" IsEnabled="False" Background="#10B981" Foreground="White"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="100" Padding="10,8" Click="Cancel_Click" Background="#EF4444" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
