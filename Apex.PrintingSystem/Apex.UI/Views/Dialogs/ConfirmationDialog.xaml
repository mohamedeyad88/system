<Window x:Class="Apex.UI.Views.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmation" Height="250" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">
    
    <Border Background="{DynamicResource Panel}" CornerRadius="12" BorderBrush="{DynamicResource Border}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.2" Color="#000"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource Ink}"/>

            <!-- Message -->
            <TextBlock Grid.Row="1" Text="{Binding Message}" FontSize="14" Foreground="{DynamicResource Muted}" 
                       TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,16"/>

            <!-- Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding CancelText}" x:Name="BtnCancel" Click="BtnCancel_Click"
                        Style="{DynamicResource Button.Secondary}" Margin="0,0,12,0" Width="100"/>
                
                <Button Content="{Binding ConfirmText}" x:Name="BtnConfirm" Click="BtnConfirm_Click"
                        Style="{DynamicResource Button.Primary}" Width="100"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
