<UserControl x:Class="Apex.UI.Views.PrinterDiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource Brush.Background}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource Diagnostics_SystemDiagnostics}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Primary}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                         <TextBlock Text="{DynamicResource Diagnostics_OverallStatus}" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="14"/>
                         <TextBlock Text="{Binding SystemStatus}" FontWeight="SemiBold" FontSize="14">
                             <TextBlock.Style>
                                 <Style TargetType="TextBlock">
                                     <Setter Property="Foreground" Value="{StaticResource Brush.Success}"/>
                                     <Style.Triggers>
                                         <DataTrigger Binding="{Binding SystemStatus}" Value="Warning: Low Disk Space">
                                             <Setter Property="Foreground" Value="{StaticResource Brush.Error}"/>
                                         </DataTrigger>
                                     </Style.Triggers>
                                 </Style>
                             </TextBlock.Style>
                         </TextBlock>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📜 View Logs" Command="{Binding OpenLogViewerCommand}" Style="{StaticResource Button.Secondary}" Margin="0,0,10,0"/>
                    <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource Button.Primary}"/>
                </StackPanel>
            </Grid>

            <!-- System Health (Disks) -->
            <Border Grid.Row="1" Background="{StaticResource Brush.Surface}" CornerRadius="8" Padding="20" Margin="0,0,0,30" Effect="{StaticResource Shadow.Small}">
                <StackPanel>
                    <!-- Section Title -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="💾 Storage Health" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}"/>
                    </StackPanel>
                    
                    <ItemsControl ItemsSource="{Binding Drives}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,15">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon -->
                                    <Border Grid.RowSpan="2" Width="40" Height="40" Background="{StaticResource Brush.Surface.Highlight}" CornerRadius="20" Margin="0,0,15,0">
                                        <TextBlock Text="HDD" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="10" FontWeight="Bold"/>
                                    </Border>

                                    <!-- Label -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Primary}" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Label}" Foreground="{StaticResource Brush.Text.Secondary}"/>
                                    </StackPanel>

                                    <!-- Usage Text -->
                                    <TextBlock Grid.Column="2" Text="{Binding UsageText}" Foreground="{StaticResource Brush.Text.Secondary}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,4"/>

                                    <!-- Progress Bar -->
                                    <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Height="6">
                                        <Border Background="{StaticResource Brush.Background.Input}" CornerRadius="3"/>
                                        <Border HorizontalAlignment="Left" CornerRadius="3" Width="{Binding UsagePercent, Converter={StaticResource PercentToWidthConverter}, RelativeSource={RelativeSource AncestorType=Grid}}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                     <Setter Property="Background" Value="#3B82F6"/> <!-- Blue Default -->
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsLowSpace}" Value="True">
                                                             <Setter Property="Background" Value="{StaticResource Brush.Error}"/>
                                                         </DataTrigger>
                                                     </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Printer Health -->
            <StackPanel Grid.Row="2">
                <TextBlock Text="🖨️ Printer Status" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}" Margin="0,0,0,15"/>
                
                <ItemsControl ItemsSource="{Binding Printers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Width="280" Margin="0,0,20,20" Padding="20">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource Brush.Text.Primary}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{DynamicResource Diagnostics_LocalDevice}" FontSize="12" Foreground="{StaticResource Brush.Text.Secondary}"/>
                                        </StackPanel>
                                        
                                        <!-- Status Badge -->
                                        <Border CornerRadius="4" Padding="8,4" Grid.Column="1" VerticalAlignment="Top">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource Brush.Success}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsOffline}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource Brush.Surface.Highlight}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource Brush.Error}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                    
                                    <Separator Background="{StaticResource Brush.Border}" Margin="0,0,0,15"/>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="QUEUE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}"/>
                                            <TextBlock Text="{Binding QueueLength}" FontSize="18" Foreground="{StaticResource Brush.Text.Primary}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="STATE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}"/>
                                            <TextBlock Text="{Binding IsOffline, Converter={StaticResource BooleanToTextConverter}, ConverterParameter='Offline|Online'}" 
                                                       FontSize="14" Foreground="{StaticResource Brush.Text.Primary}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </ScrollViewer>
    
    <!-- Local Converter resources if needed, assuming Global Converters exist or need to be added. 
         Wait, BooleanToTextConverter might not exist globally. Let's use Style Triggers for text if needed, or simple binding.
         Used 'IsOffline' triggers above. For the text below, let's just bind Status directly again or use triggers.
    -->
</UserControl>
