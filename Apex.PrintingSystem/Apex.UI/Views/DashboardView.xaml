<UserControl x:Class="Apex.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <Grid Background="{DynamicResource Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Padding="32,24">
            <StackPanel>
                <TextBlock Text="{DynamicResource Dashboard}" 
                          Style="{StaticResource Text.PageTitle}"/>
                <TextBlock Text="{DynamicResource Welcome}" 
                          Style="{StaticResource Text.Caption}"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Dashboard Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,0,32,32">
                
                <!-- Quick Stats Cards -->
                <TextBlock Text="Dashboard Overview" 
                          Style="{StaticResource Text.SectionTitle}"
                          Margin="0,0,0,16"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Card 1 -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Active Printers" FontSize="14" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                            <TextBlock Text="--" FontSize="32" FontWeight="Bold" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Card 2 -->
                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Print Jobs Today" FontSize="14" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                            <TextBlock Text="--" FontSize="32" FontWeight="Bold" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Card 3 -->
                    <Border Grid.Column="4" Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="System Status" FontSize="14" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                            <TextBlock Text="Ready" FontSize="32" FontWeight="Bold" Margin="0,8,0,0" Foreground="{DynamicResource Brush.Success}"/>
                        </StackPanel>
                    </Border>
                </Grid>
                
            </StackPanel>
        </ScrollViewer>
        
    </Grid>
</UserControl>
