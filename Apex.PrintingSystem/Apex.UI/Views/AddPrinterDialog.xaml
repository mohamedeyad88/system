<Window x:Class="Apex.UI.Views.AddPrinterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Printer" Height="500" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FFFFFF">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🖨️ Add System Printer" FontSize="22" FontWeight="Bold" Foreground="#14181D"/>
            <TextBlock Text="Select a printer from your system to add to the print management pool" FontSize="13" Foreground="#6B6864" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Printer List -->
        <Border Grid.Row="1" Background="#FAF9F7" CornerRadius="12" Padding="4" BorderBrush="#DDD9D3" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.25" Color="#000"/>
            </Border.Effect>
            <ListBox x:Name="PrinterListBox" BorderThickness="0" Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="#FFFFFF" CornerRadius="8"
                                            Padding="16,12" Margin="4" Cursor="Hand" BorderThickness="1" BorderBrush="#DDD9D3">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="🖨️" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <ContentPresenter/>
                                            </StackPanel>
                                            <Border Grid.Column="2" x:Name="CheckMark" Visibility="Collapsed"
                                                    Background="#1B7F4C" CornerRadius="10" Width="20" Height="20" VerticalAlignment="Center">
                                                <TextBlock Text="✓" Foreground="White" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#E6F3F6"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#0891B2"/>
                                            <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#DDD9D3"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="#14181D"/>
                            <TextBlock FontSize="12" Foreground="#6B6864" Margin="0,2,0,0">
                                <Run Text="{Binding Type}"/>
                                <Run Text=" • "/>
                                <Run Text="{Binding StatusText}"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="20,12" Cursor="Hand"
                    Background="#DDD9D3" Foreground="#14181D" FontWeight="SemiBold" BorderThickness="0" Margin="0,0,12,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Add Selected Printer" Click="Add_Click" Padding="20,12" Cursor="Hand"
                    Background="#0891B2" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
