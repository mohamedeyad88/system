<UserControl x:Class="Apex.UI.Views.PrinterPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="400"
             Background="#FAF9F7">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="PropLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#6B6864"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="PropValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FAF9F7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="#14181D" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="⚙️ Printer Properties" FontSize="18" FontWeight="Bold" Foreground="#FAF9F7"/>
                    <TextBlock Text="{Binding PropertiesPrinter.Name}" FontSize="14" Foreground="#6B6864" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="✕" Command="{Binding ClosePropertiesCommand}"
                        Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#6B6864" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Padding="20">
            <StackPanel>
                <!-- Status Section -->
                <Border Background="#F8FAFC" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{DynamicResource PrinterProps_Status}" Style="{StaticResource PropLabel}"/>
                            <Border HorizontalAlignment="Left" CornerRadius="4" Padding="8,4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#FEE2E2"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PropertiesPrinter.IsOnline}" Value="True">
                                                <Setter Property="Background" Value="#D1FAE5"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontWeight="SemiBold" FontSize="12">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Offline"/>
                                            <Setter Property="Foreground" Value="#B42318"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PropertiesPrinter.IsOnline}" Value="True">
                                                    <Setter Property="Text" Value="Online"/>
                                                    <Setter Property="Foreground" Value="#1B7F4C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{DynamicResource PrinterProps_Queue}" Style="{StaticResource PropLabel}"/>
                            <TextBlock Text="{Binding PropertiesPrinter.QueueLength, StringFormat={}{0} jobs pending}" Style="{StaticResource PropValue}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Basic Info -->
                <TextBlock Text="{DynamicResource PrinterProps_GeneralInfo}" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource PrinterProps_Type}" Style="{StaticResource PropLabel}"/>
                        <TextBlock Text="{Binding PropertiesPrinter.Type}" Style="{StaticResource PropValue}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource PrinterProps_PortLocation}" Style="{StaticResource PropLabel}"/>
                        <TextBlock Text="{Binding PropertiesPrinter.PoolName, TargetNullValue='USB/Local'}" Style="{StaticResource PropValue}"/>
                    </StackPanel>
                </Grid>

                <!-- Capabilities -->
                <TextBlock Text="{DynamicResource PrinterProps_Capabilities}" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <WrapPanel Margin="0,0,0,20">
                    <Border Background="#E0E7FF" CornerRadius="6" Padding="10,6" Margin="0,0,8,8"
                            Visibility="{Binding PropertiesPrinter.Capabilities.CanPrintColor, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="🎨 Color Printing" Foreground="#4338CA" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="#F3E8FF" CornerRadius="6" Padding="10,6" Margin="0,0,8,8"
                            Visibility="{Binding PropertiesPrinter.Capabilities.CanDuplex, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="📄 Duplex (Double-sided)" Foreground="#7E22CE" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    
                    <!-- Paper Sizes -->
                    <ItemsControl ItemsSource="{Binding PropertiesPrinter.SupportedPapers}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#14181D" CornerRadius="6" Padding="10,6" Margin="0,0,8,8">
                                    <TextBlock Text="{Binding}" Foreground="#6B6864" FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </WrapPanel>

                <!-- Warnings -->
                <!-- Warnings -->
                <StackPanel Visibility="{Binding PropertiesPrinter.IsOutOfPaper, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource PrinterProps_OutOfPaper}" Foreground="#B91C1C" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Actions -->
        <Border Grid.Row="2" BorderBrush="#14181D" BorderThickness="0,1,0,0" Padding="20" Background="#F8FAFC">
            <StackPanel>
                <Button Content="🖨️ Print Test Page" 
                        Command="{Binding TestPrintCommand}" 
                        CommandParameter="{Binding PropertiesPrinter}"
                        Height="40" Background="#0891B2" Foreground="White" BorderThickness="0" Margin="0,0,0,10">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="🗑️ Flush Print Queue" 
                        Command="{Binding FlushQueueCommand}" 
                        CommandParameter="{Binding PropertiesPrinter}"
                        Height="40" Background="#B42318" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
