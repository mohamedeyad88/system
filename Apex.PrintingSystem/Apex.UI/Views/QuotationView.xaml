<UserControl x:Class="Apex.UI.Views.QuotationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F8FAFC">
    
    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.08" Color="#000"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Input Form -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Padding="32">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,24">
                        <Run Text="💰 "/><Run Text="{DynamicResource Quotation_Title}"/>
                    </TextBlock>
                    
                    <!-- Basic Details -->
                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,16">
                        <Run Text="📄 "/><Run Text="{DynamicResource Quotation_BasicDetails}"/>
                    </TextBlock>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Total Pages -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{DynamicResource Quotation_TotalPages}" Style="{StaticResource InputLabel}"/>
                            <TextBox Text="{Binding TotalPages, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}"/>
                        </StackPanel>
                        
                        <!-- Quantity -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{DynamicResource Quotation_Quantity}" Style="{StaticResource InputLabel}"/>
                            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Double Sided -->
                    <CheckBox IsChecked="{Binding IsDoubleSided}" Content="{DynamicResource Quotation_DoubleSided}" Margin="0,16,0,0" FontSize="14"/>
                    
                    <!-- Price Per Sheet -->
                    <StackPanel Margin="0,20,0,0">
                        <TextBlock Text="{DynamicResource Quotation_PricePerSheet}" Style="{StaticResource InputLabel}"/>
                        <TextBox Text="{Binding PricePerSheet, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}"/>
                    </StackPanel>
                    
                    <!-- Cover -->
                    <Separator Margin="0,24,0,24" Background="#E2E8F0"/>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,16">
                        <Run Text="📕 "/><Run Text="{DynamicResource Quotation_CoverOptions}"/>
                    </TextBlock>
                    
                    <CheckBox IsChecked="{Binding HasCover}" Content="{DynamicResource Quotation_AddCover}" FontSize="14" Margin="0,0,0,12"/>
                    
                    <StackPanel Visibility="{Binding HasCover, Converter={StaticResource BoolToVisConverter}}">
                        <TextBlock Text="{DynamicResource Quotation_CoverPrice}" Style="{StaticResource InputLabel}"/>
                        <TextBox Text="{Binding CoverPrice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}"/>
                    </StackPanel>
                    
                    <!-- Finishing Services -->
                    <Separator Margin="0,24,0,24" Background="#E2E8F0"/>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,16">
                        <Run Text="✨ "/><Run Text="{DynamicResource Quotation_FinishingServices}"/>
                    </TextBlock>
                    
                    <ItemsControl ItemsSource="{Binding AvailableServices}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="#1E293B"/>
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#64748B" Visibility="{Binding Description, Converter={StaticResource NullToVisConverter}}"/>
                                        </StackPanel>
                                        
                                        <TextBlock Grid.Column="2" FontWeight="Bold" FontSize="14" Foreground="#3B82F6" VerticalAlignment="Center">
                                            <Run Text="{Binding Price, StringFormat='{}{0:F2}'}"/>
                                            <Run Text=" EGP"/>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <!-- Profit Margin -->
                    <Separator Margin="0,24,0,24" Background="#E2E8F0"/>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,16">
                        <Run Text="📈 "/><Run Text="{DynamicResource Quotation_ProfitMargin}"/>
                    </TextBlock>
                    
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Quotation_ProfitMarginPercent}" Style="{StaticResource InputLabel}"/>
                        <TextBox Text="{Binding ProfitMargin, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputBox}"/>
                    </StackPanel>
                    
                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,32,0,0">
                        <Button Command="{Binding ResetCommand}" Background="#E2E8F0" Foreground="#475569" Padding="20,12" Margin="0,0,12,0" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand">
                            <TextBlock><Run Text="🔄 "/><Run Text="{DynamicResource Quotation_Reset}"/></TextBlock>
                        </Button>
                        <Button Command="{Binding ExportQuoteCommand}" Background="#3B82F6" Foreground="White" Padding="20,12" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand">
                            <TextBlock><Run Text="📄 "/><Run Text="{DynamicResource Quotation_Export}"/></TextBlock>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- RIGHT: Live Results -->
        <Border Grid.Column="2" Style="{StaticResource Card}" Padding="24">
            <StackPanel>
                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20">
                    <Run Text="📊 "/><Run Text="{DynamicResource Quotation_CostBreakdown}"/>
                </TextBlock>
                
                <!-- Sheets Required -->
                <Border Background="#EFF6FF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Quotation_SheetsRequired}" FontSize="11" Foreground="#64748B"/>
                        <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1E40AF">
                            <Run Text="{Binding SheetsRequired}"/>
                            <Run Text=" " FontSize="14"/><Run Text="{DynamicResource Quotation_Sheets}" FontSize="14"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
                <!-- Cost Items -->
                <StackPanel Margin="0,16,0,0">
                    <!-- Printing Cost -->
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Quotation_PrintingCost}" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding PrintingCost, StringFormat='{}{0:F2} EGP'}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <!-- Cover Cost -->
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Quotation_CoverCost}" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding CoverCost, StringFormat='{}{0:F2} EGP'}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <!-- Finishing Cost -->
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Quotation_FinishingCost}" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding FinishingCost, StringFormat='{}{0:F2} EGP'}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <Separator Background="#E2E8F0" Margin="0,12,0,12"/>
                    
                    <!-- Subtotal -->
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Quotation_Subtotal}" FontSize="14" FontWeight="SemiBold" Foreground="#475569"/>
                        <TextBlock Text="{Binding SubTotal, StringFormat='{}{0:F2} EGP'}" FontSize="16" FontWeight="Bold" Foreground="#475569" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <!-- Profit -->
                    <Grid Margin="0,0,0,12">
                        <TextBlock FontSize="13" Foreground="#047857">
                            <Run Text="{DynamicResource Quotation_Profit}"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding ProfitMargin}"/>
                            <Run Text="%)"/>
                        </TextBlock>
                        <TextBlock Text="{Binding ProfitAmount, StringFormat='{}{0:F2} EGP'}" FontSize="14" FontWeight="SemiBold" Foreground="#047857" HorizontalAlignment="Right"/>
                    </Grid>
                    
                    <Separator Background="#E2E8F0" Margin="0,12,0,12"/>
                    
                    <!-- Final Price -->
                    <Border Background="#FEF2F8" CornerRadius="8" Padding="16">
                        <Grid>
                            <TextBlock Text="{DynamicResource Quotation_FinalPrice}" FontSize="16" FontWeight="Bold" Foreground="#3B82F6"/>
                            <TextBlock Text="{Binding FinalPrice, StringFormat='{}{0:F2} EGP'}" FontSize="20" FontWeight="Bold" Foreground="#3B82F6" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </StackPanel>
                
                <!-- Detailed Breakdown (collapsible) -->
                <Expander FontSize="13">
                    <Expander.Header>
                        <TextBlock><Run Text="📝 "/><Run Text="{DynamicResource Quotation_DetailedBreakdown}"/></TextBlock>
                    </Expander.Header>
                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding Breakdown}" FontFamily="Consolas" FontSize="11" Foreground="#64748B" Background="#F8FAFC" Padding="12" xml:space="preserve"/>
                    </ScrollViewer>
                </Expander>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
