<UserControl x:Class="Apex.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:Apex.UI.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="#F8FAFC">
    
    <UserControl.Resources>
        <!-- Modern Card -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.08" Color="#000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>

        <!-- Section Title -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Field Label -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock FontSize="28" FontWeight="Bold" Foreground="#0F172A">
                <Run Text="⚙️ "/><Run Text="{DynamicResource Settings_Title}"/>
            </TextBlock>
            <TextBlock Text="{DynamicResource Settings_Description}" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid MaxWidth="800" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Company Info Card -->
                <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="🏢" FontSize="20" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Settings_CompanyInfo}" Style="{StaticResource SectionTitle}" Margin="0"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_CompanyName}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding CompanyName}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_Address}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding CompanyAddress}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_Phone}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding CompanyPhone}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Settings_Logo}" Style="{StaticResource FieldLabel}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding LogoPath}" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                                <Button Grid.Column="1" Content="📂" Command="{Binding BrowseLogoCommand}" 
                                        Style="{StaticResource SecondaryButton}" Width="44"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Financial Defaults Card -->
                <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="💰" FontSize="20" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Settings_Financial}" Style="{StaticResource SectionTitle}" Margin="0"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_PricePerPage}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding DefaultPricePerPage}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_CoverPrice}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding DefaultCoverPrice}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="{DynamicResource Settings_InvoiceHeader}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding InvoiceHeader}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                        
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Settings_InvoiceFooter}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding InvoiceFooter}" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- System Settings Card -->
                <Border Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource ModernCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Language -->
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="🌐" FontSize="20" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource Settings_LanguageRegion}" Style="{StaticResource SectionTitle}" Margin="0"/>
                            </StackPanel>
                            
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_ApplicationLanguage}" Style="{StaticResource FieldLabel}"/>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="4">
                                    <ComboBox SelectedValue="{Binding SelectedLanguage}" SelectedValuePath="Tag" 
                                              BorderThickness="0" Background="Transparent" FontSize="14" Padding="8,6">
                                        <ComboBoxItem Content="🇺🇸 English" Tag="en"/>
                                        <ComboBoxItem Content="🇸🇦 العربية" Tag="ar"/>
                                    </ComboBox>
                                </Border>
                            </StackPanel>
                        </StackPanel>

                        <!-- Backup -->
                        <StackPanel Grid.Column="2">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="💾" FontSize="20" Margin="0,0,8,0"/>
                                <TextBlock Text="{DynamicResource Settings_Backup}" Style="{StaticResource SectionTitle}" Margin="0"/>
                            </StackPanel>
                            
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_BackupPath}" Style="{StaticResource FieldLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding BackupPath}" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                                    <Button Grid.Column="1" Content="📂" Command="{Binding BrowseBackupPathCommand}" 
                                            Style="{StaticResource SecondaryButton}" Width="44"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Save Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Command="{Binding SaveSettingsCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" Padding="28,14" Cursor="Hand">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="White">
                    <Run Text="💾 "/><Run Text="{DynamicResource Settings_Save}"/>
                </TextBlock>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
