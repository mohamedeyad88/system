<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- ========================================
         MODERN EFFECTS LIBRARY
         Glassmorphism, Neumorphism, Advanced Effects
         ======================================== -->
    
    <!-- Glassmorphism Effect -->
    <Style x:Key="Glass.Card" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="#FFFFFF" Opacity="0.7"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush">
            <Setter.Value>
                <SolidColorBrush Color="#FFFFFF" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Direction="0" Color="#000000" Opacity="0.1"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Glossy Button Effect -->
    <Style x:Key="Button.Glossy" TargetType="Button">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#3B82F6" Offset="0"/>
                    <GradientStop Color="#2563EB" Offset="0.5"/>
                    <GradientStop Color="#1E40AF" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="600"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Color="#1E40AF" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Color="#1E40AF" Opacity="0.3"/>
                        </Border.Effect>
                        <!-- Inner Highlight (Glossy Effect) -->
                        <Grid>
                            <Border CornerRadius="8,8,0,0" Height="50" VerticalAlignment="Top">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#4DFFFFFF" Offset="0"/>
                                        <GradientStop Color="Transparent" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            <Setter TargetName="border" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Frosted Glass Panel -->
    <Style x:Key="Panel.Frosted" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="#F9FAFB" Opacity="0.85"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush">
            <Setter.Value>
                <SolidColorBrush Color="#FFFFFF" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <BlurEffect Radius="10"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Premium Shadow Levels -->
    <DropShadowEffect x:Key="Shadow.Elevated" BlurRadius="32" ShadowDepth="8" Direction="270" Color="#000000" Opacity="0.2"/>
    <DropShadowEffect x:Key="Shadow.Floating" BlurRadius="40" ShadowDepth="12" Direction="270" Color="#000000" Opacity="0.25"/>
    
    <!-- Soft Neumorphism Style (Light Mode) -->
    <Style x:Key="Neuro.Card.Light" TargetType="Border">
        <Setter Property="Background" Value="#E0E5EC"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="20" ShadowDepth="10" Direction="315" Color="#A3B1C6" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Glowing Border Effect -->
    <Style x:Key="Border.Glow.Primary" TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="16" ShadowDepth="0" Direction="0" Color="#3B82F6" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Subtle Gradient Overlay -->
    <LinearGradientBrush x:Key="Overlay.Gradient.Subtle" StartPoint="0,0" EndPoint="0,1" Opacity="0.05">
        <GradientStop Color="#000000" Offset="0"/>
        <GradientStop Color="Transparent" Offset="0.5"/>
        <GradientStop Color="#000000" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Shimmer Effect for Loading -->
    <LinearGradientBrush x:Key="Shimmer.Gradient" StartPoint="0,0.5" EndPoint="1,0.5">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#F3F4F6" Offset="0"/>
            <GradientStop Color="#E5E7EB" Offset="0.5"/>
            <GradientStop Color="#F3F4F6" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    
</ResourceDictionary>
